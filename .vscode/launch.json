{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to PowerShell Functions host",
      "type": "coreclr",
      "request": "attach",
      "processId": "${command:azureFunctions.pickProcess}"
    }
  ]
}
